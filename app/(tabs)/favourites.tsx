import { View, Text } from 'react-native';
import { Container } from '~/components';

const Favourites = () => {
  return (
    <Container>
      <Text style={{ color: 'white' }}>Favs</Text>
    </Container>
  );
};

export default Favourites;
